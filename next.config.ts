import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["qrplocice.s3.eu-north-1.amazonaws.com", 'img.favpng.com'],
  },
};

export default nextConfig;
